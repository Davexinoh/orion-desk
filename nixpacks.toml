providers = ["python", "node"]

[phases.build]
cmds = [
  "npm ci --prefix web",
  "npm run build --prefix web",
]

[start]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --app-dir server"
