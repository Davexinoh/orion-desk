{
  "name": "orion-desk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:web": "npm run dev --prefix web",
    "dev:server": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8787 --app-dir server",
    "build": "npm run build --prefix web"
  }
}
